<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="/styles/style.css">
	<link rel="stylesheet" href="/styles/shop-product-editor.css">
	<title><%= title %></title>
</head>
<body>
	<%- include("partials/header") %>
	<main>
		<div id="product-form" class="panel">
			<h1><%= title %></h1>
			<p>£<%= variants[0].price %></p>
			<img src="<%= images[0] != null ? images[0].src : '' %>">
			<p><%= body_html %></p>
			<a href="/shop/<%= api_id %>/editor">Go to editor.</a>
		</div>
	</main>
</body>
</html>